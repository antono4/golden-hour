interface:
  display_name: "Golden Hour"
  short_description: "Repo-specific patterns and workflows for golden-hour"
  default_prompt: "Use the golden-hour repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true